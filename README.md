## Usage

Linux users:
If it doesn't work, run it in the folder where the file is.
If you don’t want to `cd /path/to/it`, then save it in your home directory.

Run this in your terminal:

```bash
chmod +x Ubuntu-64bit
./Ubuntu-64bit
```

## IMPORTANT

Make sure this kind of automation is allowed on your server before using this.

THIS ONLY PASTES FROM YOUR CLIPBOARD IN CHAT.
Copy the sell command for your server before running the app.

## Keybinds

* **Shift**: Pause
* **Shift again**: Unpause
* **Esc**: Closes the app
